version: 2

conda:
  environment: vampyr-docs.yml

# build platform
build:
  os: ubuntu-22.04

# Python environment
python:
 install:
   - method: pip
     path: .
     extra_requirements:
       - docs

# how to run sphinx
sphinx:
  configuration: docs/source/conf.py
  builder: html

